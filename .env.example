# TGM Ventures Environment Variables
# Copy this file to .env.local and fill in the actual values

# Firebase API Key (required)
# Get this from: https://console.firebase.google.com/project/tgm-ventures-site/settings/general
# Or from Google Secret Manager: gcloud secrets versions access latest --secret="FIREBASE_API_KEY"
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here

# Note: Never commit .env.local to version control!
# The Firebase API key is stored securely in Google Secret Manager for production use.
# For local development, you can either:
# 1. Use the scripts/start-local.sh script which fetches from Secret Manager automatically
# 2. Manually copy the key to .env.local
